# machine-local — never commit
.claude-state.json
.pipeline-off
.ts-arch-off
.secret-scan-off
*.phalanx.bak.*
*.bak
.DS_Store
node_modules/
TASKS.md
PROGRESS.md
.claude-runs/
.work-off
.phalanx-repos
# per-deployment opt-in markers (autorun/automerge/deploy) — NEVER commit: a
# published marker would make every clone auto-run/merge/deploy.
.phalanx-*

# per-pass worktrees (Phalanx loop)
.claude/worktrees/
