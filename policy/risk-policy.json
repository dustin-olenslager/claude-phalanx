{
  "version": 1,
  "_comment": "Phalanx policy contract (reserved for future versioned gate config). Gates read their own behavior from embedded defaults today; this file is kept so install.sh has a contract to validate and downstream tooling has a stable anchor."
}
