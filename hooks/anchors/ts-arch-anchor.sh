#!/usr/bin/env bash
# SessionStart: inject typed-error + Clean Architecture directive (CLAUDE.md §14/§3) every session.
cat <<'EOF'
{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"EFFECT-TS + CLEAN-ARCHITECTURE ACTIVE (always-on, CLAUDE.md §14/§3). (A) Any TypeScript (.ts/.tsx/.mts/.cts) you or a subagent write MUST first consult the effect-ts skill and default to Effect 3.x (https://effect.website): Effect<A,E,R> for I/O, Effect.tryPromise instead of raw await, Data.TaggedError typed errors instead of throw, Effect.Service/Layer for DI, effect/Schema at boundaries, runPromise/runFork at ONE entrypoint; tests via @effect/vitest + test Layers + fast-check. Trivial pure glue stays plain TS (YAGNI). Other languages use their typed-error + schema + arch-linter stack (Python: Returns+Pydantic+import-linter; Kotlin: Arrow+ArchUnit; Rust/Go: native Result/errors + clippy/golangci-lint). (B) Any code PLANNED or WRITTEN (any language) MUST apply Clean Architecture via the clean-architecture skill: deps inward only, business rules free of framework/IO imports, every external concern behind a port + edge adapter, DTOs across boundaries, one composition root. ENFORCED at edit-time by the ts-arch gate AND mechanically at verify by arch-enforce (dependency-cruiser/import-linter/ArchUnit). Override: 'stop effect' / 'stop clean-arch' -> touch <CLAUDE_DIR>/.ts-arch-off."}}
EOF
