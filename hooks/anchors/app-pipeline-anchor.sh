#!/usr/bin/env bash
# SessionStart: inject the app-build pipeline directive (CLAUDE.md §13) every session.
cat <<'EOF'
{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"APP-BUILD PIPELINE ACTIVE (always-on, CLAUDE.md §13). Non-trivial build moves through phases, each forcing a skill: (1) brainstorm/PRD -> product-management (write-spec/brainstorm). (2) research -> deep-research. (3) architecture -> system-design THEN adr-kit (one ADR per non-trivial decision). (4) plan -> phased-plan. (5) UI/UX -> frontend-design, ship web+mobile same task, Playwright @390+1440, WCAG-AA. (6) implement -> ponytail (YAGNI) + caveman + observability (§12). (7) LLM/API -> route via gateway, don't hardwire a provider. (8) MCP -> mcp-builder. (9) cleanup -> simplify + ponytail. (10) review -> edge-hunter THEN adversary-review (§16). (11) security -> security-review + secret-scan. (12) verify -> verify + run + tsc --noEmit + lint + Playwright + arch-enforce. (13) commit -> caveman-commit. (14) memory -> consolidate-memory. ENFORCED: no code edit before a plan/spec; no git commit before a verify/test/typecheck/lint. Missing skill -> do the phase manually, never block. Override: 'stop pipeline' -> touch <CLAUDE_DIR>/.pipeline-off."}}
EOF
